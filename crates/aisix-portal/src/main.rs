//! `aisix-portal` —— 外部用户的自助门户。
//!
//! 与 `aisix-console` **分进程**：控制台是一个共享口令的全权管理端，不能同时
//! 服务陌生人。角色判断错一次就是全量泄漏；进程分开后，门户里根本不存在配置
//! 编辑的代码路径。
//!
//! 网关零改动：门户只读指标、只写自己的账本，以及在余额归零时把密钥置
//! `disabled`。它**绝不**出现在网关的请求路径上。

mod store;

#[tokio::main]
async fn main() {
    eprintln!("aisix-portal: 骨架就位（Task 1）");
}
